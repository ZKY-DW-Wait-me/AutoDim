# CHANGELOG

## 2026-08 Phase A：复杂图纸清洗管线（当前主线）

目标：让"扫描矢量化/碎片化"的复杂 2D 图纸也能自动标注。

### 新增

- **AutoDim.Core**（纯 C# 几何库，无 AutoCAD 依赖、无 NuGet 包，可独立单测）：
  - 清洗管线：端点吸附 → 去重 → 共线微段合并（圆弧以 bulge 单段保留）→ 平面图闭合轮廓提取 → 圆去重。
  - 噪声过滤：短线段（不属于闭合面且 <3mm）、碎环（面积 <1mm²）、细长碎面（周长²/面积 >60）。
  - 特征分组：按空间邻近把闭合面+圆聚成特征簇。
  - 自测：`dotnet run --project test/AutoDim.Core.SelfTest`（19 项，覆盖去重/合并/内外孔/圆角 bulge/分组/噪声过滤）。
- **ADIMCLEAN** 命令：脏图 → 清洗 → 绘制到 `ADIM_CLEAN` 图层 → 按特征组分别自动标注（一条龙）。
  - 组标注策略：纯孔组只标孔；全小碎面组只标总体+孔；混合组最大面+孔标全部、其余小面只标总体。
  - 孔标注改进：直径线方向轮流避开定位链；定位链过密（>11 段或间距 <8mm）自动跳过。
  - 组级清场：每组只 purge 一次，重复运行幂等、互不误擦。
  - 输出质量指标：标注总量 + 真实重叠对数（正面积相交、排除同孔内部假阳性）。
- **ADIMCFG C**：自动清洗开关——开启后 `AUTODIM/ADIMALL/ADIMWIN/ADIMSEL` 先清洗再标注。
- **ADIMCLN**：清洗参数（吸附/合并公差、最短保留长度、最小面积、最大细长度）持久化到图，按图纸松紧可调。

### 无界面回归测试修复（test/run-tests.sh，== PASS ==）

- `SECURELOAD=1` 静默拒绝 NETLOAD → 脚本先置 0。
- 模板 `acadiso.dwt` 首次初始化卡控制台 → 输入改用仓库内 `test.dwg`。
- accoreconsole 从 Git Bash(MSYS) 启动不读脚本 → 经 `run-headless.ps1` 用 PowerShell 启动。
- PS1 含中文注释（无 BOM UTF-8）导致 PowerShell 5.1 解析失败 → 脚本保持纯 ASCII。
- Git Bash sed 吃掉 Windows 路径反斜杠 → 用 heredoc 生成脚本。

### test.dwg 实测（默认参数，2026-08）

```text
输入线段(含块展开)  8975
清洗后              6020
闭合面(含圆弧bulge) 333
去重圆              447
特征组              123
自动标注总量        826
真实重叠对          544
无界面运行耗时      约 10s
```

（`ADIMCLN` 调松参数后：标注 758、重叠 434——不同图纸松紧不同。）

## 2026-07 初版（Phase 0-5）

- AutoCAD 2025 / .NET 8 自动尺寸标注插件，五种标注类别 + 三种触发方式 + 坐标标注模式。
- 复杂图纸无法稳定标注，项目暂停个人开发、转为公开仓库（MIT）。

## 已知限制与下一步

- 尺寸避让布局优化（当前真实重叠 544 对，密集区仍可能互相压线）——最大剩余课题。
- MLLM 语义层（识别"哪片是轮廓/孔/填充噪声"，MechVQA 一类能力）尚未接入。
- 3D(STEP) 与 MechRAG 式知识库路线待有 3D 资产后启动。
