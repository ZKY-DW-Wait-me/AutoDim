# CHANGELOG

## 2026-08 Phase A：复杂图纸清洗管线（当前主线）

目标：让"扫描矢量化/碎片化"的复杂 2D 图纸也能自动标注。

### 新增

- **标注布局收尾（LayoutSolver）+ 生成期防撞**：真实文字撞车 916 对 → **48 对**（test.dwg）：
  - **整区清场**：逐组清场在相邻组很近时会误擦对方标注（test.dwg 实测 824 个标注只剩 644 个，
    180 个被静默删掉），改为清洗区域整区 6×gap 清场 + 布局收尾外推上限 2.5×原始偏移，
    重复运行幂等且不再丢标注。
  - **重复直径合并为数量注记（GB "N×Ød"）**：test.dwg 的 447 个孔只有 6 种直径
    （128×Ø2.75、128×Ø3.5、128×Ø4.25、48×Ø2.5…），原来每个孔都标 ⌀，直径标注 442 个
    减到 111 个；同径 ≥2 时只出 MText 注记，注记之间按 2×字高错开。
  - **斜边主投影**：斜边不再同时标水平投影+垂直投影（双投影尺寸界线盒在角部必然相交），
    只标较大的直角边（浅斜边标水平投影、陡斜边标垂直投影），另一坐标由闭合尺寸链隐含。
  - **代表孔引线扇形展开**：同一组多个不同直径的独苗孔，引线按自然角排序后强制最小
    90° 夹角展开，避免文字叠罗汉。
  - **去重**：完全重复（同测量段+同尺寸线位置）只留一个；外推后仍重叠的近重复
    （同一测量段 ≤1.5mm，来自重复/重叠面）只保留测量值较大者。test.dwg 共去重 47 个。
  - **外推避让**：剩余重叠按"远离被测要素"方向逐轮外推（尺寸线/径向引线外移），
    上限 2.5×原始偏移，test.dwg 每轮约 1200~1650 次移动。
  - **文字撞车指标**：新增"ADIM 文字撞车对数"（取标注块内文字实体外接框）作为真实可读性
    指标，替代只看 AABB 重叠（AABB 对长尺寸链/投影的交叉虚高，如 X/Y 定位链在角部
    交叉但图形并不相碰）。
- **ADIMDUMP** 诊断命令：非交互输出 ADIM 图层所有标注的类型/文字/测量点/文字框/外接框，
  供无界面基准与布局分析。
- 分段最短长度下限 0.5mm → **2mm**：过滤矢量化噪声的毫米级碎段。
- **定位链微段跳过**：链段跨度 < 3mm（文字宽约 3.5mm，放不下会被外置到界线外、
  与相邻链段文字互碰）不再标注该段——test.dwg 标注 484→344、文字撞车 48→17。
- **径向外推修复**：RadialDimension.LeaderLength 属性读取恒为 0（accoreconsole/API 限制），
  导致径向标注的外推从未生效（offset=0 恒判"已到上限"）。改为按"文字中心到圆心"实测距离
  计算偏移、写入后验证，并把外推上限从 2.5× 提到 4×——test.dwg 文字撞车 17→5，
  含 ADIMSAMPLE 的回归图文字撞车 0。
- **幂等性修复**：ADIM_CLEAN_L（开放长链可视化层）未列入辅助层排除名单，上一次运行画的
  开放链会被下一次运行当输入回喂（test.dwg 第二次运行输入 8975→10537），已修复；
  `test/run-tests.sh` 的幂等断言升级为真正连续两次 ADIMCLEAN 对比。

- **AutoDim.Core**（纯 C# 几何库，无 AutoCAD 依赖、无 NuGet 包，可独立单测）：
  - 清洗管线：端点吸附 → 去重 → 共线微段合并（圆弧以 bulge 单段保留）→ 平面图闭合轮廓提取 → 圆去重。
  - 噪声过滤：短线段（不属于闭合面且 <3mm）、碎环（面积 <1mm²）、细长碎面（周长²/面积 >60）。
  - 特征分组：按空间邻近把闭合面+圆聚成特征簇。
  - 自测：`dotnet run --project test/AutoDim.Core.SelfTest`（19 项，覆盖去重/合并/内外孔/圆角 bulge/分组/噪声过滤）。
- **ADIMCLEAN** 命令：脏图 → 清洗 → 绘制到 `ADIM_CLEAN` 图层 → 按特征组分别自动标注（一条龙）。
  - 组标注策略：纯孔组只标孔；全小碎面组只标总体+孔；混合组最大面+孔标全部、其余小面只标总体。
  - 孔标注改进：直径线方向轮流避开定位链；定位链过密（>11 段或间距 <8mm）自动跳过。
  - 组级清场：每组只 purge 一次，重复运行幂等、互不误擦。
  - 输出质量指标：标注总量 + 真实重叠对数（正面积相交、排除同孔内部假阳性）。
- **ADIMCFG C**：自动清洗开关——开启后 `AUTODIM/ADIMALL/ADIMWIN/ADIMSEL` 先清洗再标注。
- **ADIMCLN**：清洗参数（吸附/合并公差、最短保留长度、最小面积、最大细长度）持久化到图，按图纸松紧可调。
- 布局微调：分段尺寸外推 0.8×gap；孔直径线从孔簇质心向外辐射（真实重叠 544→530）。
- `bench.sh`：test.dwg 基准脚本（标注总量/真实重叠/耗时），供后续改动量化对比。
- 开放链闭合（AutoDim.Core.ChainLinking）：悬空端点最近优先连接（间距 ≤ 0.02% 包围盒对角线），
  扫描外轮廓碎片闭合成特征（test.dwg 闭合面 333→361、真实重叠 530→505）。

### 无界面回归测试修复（test/run-tests.sh，== PASS ==）

- `SECURELOAD=1` 静默拒绝 NETLOAD → 脚本先置 0。
- 模板 `acadiso.dwt` 首次初始化卡控制台 → 输入改用仓库内 `test.dwg`。
- accoreconsole 从 Git Bash(MSYS) 启动不读脚本 → 经 `run-headless.ps1` 用 PowerShell 启动。
- PS1 含中文注释（无 BOM UTF-8）导致 PowerShell 5.1 解析失败 → 脚本保持纯 ASCII。
- Git Bash sed 吃掉 Windows 路径反斜杠 → 用 heredoc 生成脚本。

### test.dwg 实测（默认参数，2026-08）

```text
输入线段(含块展开)  8975
清洗后              6298
闭合面(含圆弧bulge) 361（含开放链近距断口闭合 +28）
去重圆              447
特征组              136
自动标注总量        344 个尺寸 + 61 个 N×Ød 注记（孔直径 442→111）
文字撞车对数        5（真实文字外接框相交，均为 <2.5mm² 微碰）
布局收尾            去重 47 / 外推避让 ~1200~1650 次
无界面运行耗时      约 17s
```

（此前 33f59f0 的 824 个标注 / 505 重叠对中，约 180 个标注被"逐组清场"跨组误擦，
即旧数字建立在丢标注的基础上；本轮修复后所有标注完整保留，文字撞车才是真实质量信号。）

## 2026-07 初版（Phase 0-5）

- AutoCAD 2025 / .NET 8 自动尺寸标注插件，五种标注类别 + 三种触发方式 + 坐标标注模式。
- 复杂图纸无法稳定标注，项目暂停个人开发、转为公开仓库（MIT）。

## 已知限制与下一步

- 布局避让已重构（文字撞车 17 对，均为 <1.5mm² 的文字微碰：11 对为不同组的
  直径代表孔引线文字邻近，4 对为重复面的分段尺寸文字）。
- MLLM 语义层（识别"哪片是轮廓/孔/填充噪声"，MechVQA 一类能力）尚未接入——
  需要装 python 视觉模型，是下一阶段的自然起点。
- 3D(STEP) 与 MechRAG 式知识库路线待有 3D 资产后启动。
- MLLM 语义层（识别"哪片是轮廓/孔/填充噪声"，MechVQA 一类能力）尚未接入。
- 3D(STEP) 与 MechRAG 式知识库路线待有 3D 资产后启动。
