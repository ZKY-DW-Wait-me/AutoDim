# CHANGELOG

## 2026-08（v0.1.0 后续修复）

### 修复

- 标注文字样式恢复国标字体 gbeitc.shx + gbcbig.shx：上一版误把 ADIM 文字样式与圆直径引出线改成 Arial(TTF)，本版全部改回，全图标注（含圆直径引出线文字）字体统一为国标机械字体
- 已存在旧图纸里的 AutoDimText 样式也会被强制更正为国标字体，无需手动重建样式
- 圆直径引出线箭头/斜线随孔尺寸自适应：小孔配小箭头（≈0.9×孔半径，不超过默认箭头）、45° 斜线随箭头成比例（≈2.2×箭头）——test3 半径 0.78mm 的孔不再出现 1.25mm 大箭头 + 6.7mm 长斜线
- 圆直径引出线水平线长度按文字实际渲染宽度自适应（GeometricExtents 读宽 + 两端余量），不再用固定 10mm——test3 的 8×Ø1.65 水平线从 10mm 缩到 5.8mm，文字始终居中在线上方

### 开放轮廓/简单图标注（test4、单圆、单线）

- 独立 LINE 分段标注支持：OutlineDimensioner 原来只认 Polyline，用 LINE 画的开放轮廓/散线（test4 的 81 条开放线）一个尺寸都不标；现在 LINE 逐条标长度（水平/垂直/斜边主投影），与面共享边去重
- ADIMCLEAN 开放线段纳入标注：清洗出的开放线（不属于闭合面的段）独立成组标分段尺寸——单条线不再 0 尺寸，断口轮廓/散线轮廓也能出线性尺寸
- 矩形 0 尺寸 bug：底/顶整宽边被"最外边"跳过分段、同时 skipW/skipH 又跳过总体，两边互踢导致简单矩形一个尺寸都不标；现在单条整边时总体必须标，只有多段拼接覆盖才跳过总体——test 4 条线画的矩形正常标出 100×50

### 圆直径标注形式（GB 标准直径注法）

- 大圆（直径能容纳文字+两端箭头）改用 DiametricDimension：尺寸线过圆心、两端实心箭头指向圆周、文字在中间上方，不再一律折线引出
- 小孔（文字放不下，如 test3 的 Ø1.55/Ø1.65）保留折线引出线注法
- 过圆心判定阈值放宽：旧版 +2mm 间隙太苛刻，Ø6 这类中等圆被误归引出线；现在文字+两端箭头+0.5mm 间隙放得下即过圆心（Ø6/Ø10/Ø40 实测全部过圆心两端箭头）
- 圆直径标注统一为 CAD 原生 DiametricDimension：尺寸线过圆心、标准实心箭头、文字放不下时由 AutoCAD 自动外置到圆外（此前一度手绘 Line/Solid 箭头，用户指出应调用 CAD 标注，已全部弃用手绘）

### 标注全部使用 CAD 原生标注实体

- 全部标注统一为 CAD 标注实体（DIMENSION）：线性 Aligned/Rotated、直径 Diametric、半径 Radial、角度 LineAngular、坐标 Coordinate——箭头/文字/线型全部由 CAD 渲染，不再手绘 Line/Solid/MText（代码审计确认：唯一非标注的手绘实体是圆心十字中心线，属辅助线）
- 圆直径标注改用 CAD 原生 DiametricDimension：尺寸线过圆心、标准箭头、文字放不下时自动外置到圆外（AutoCAD 标准行为）
- 新增 ADIMCFG 选项 T（模板样式）：标注样式来源可切为"当前模板"（当前标注样式 + 当前图层，完全跟随用户调好的图纸模板）或"ADIM 国标样式"（插件默认，集成国标字体/自适应比例）；ADIMVER 显示当前来源

### 阵列/对称查找重做（test3 16 孔板）

- 直径分桶改为容差合并（0.005mm 半径）：SW 导出的浮点误差（0.8249999999999886 vs 0.825000000000017）不再把同一规格孔劈成 6+2 两个桶
- 同一直径的孔按空间位置聚类分组（最小生成树 + 最大间隙比切断）：远处独立孔/孔组分开标注——test3 的 1.55 孔拆成"下方 6 个 + 上方对称 2 个"，不再合并成 8×Ø1.55
- 镜像对称组自动合并：左右对称的两组 2×2 阵列合并为 8×Ø1.65，对称单孔对合并为 2×Ø1.55，符合国标对称件注法
- 圆直径引出线按孔在图形中的左右位置自动选择引出方向，右侧孔不再把文字和引出线甩出图外

## v0.1.0（2026-08）— 首个公开 Demo

面向 SolidWorks / CAD 导出的干净工程图，自动生成国标化尺寸标注。

### 功能

- 总体外形尺寸（宽/高），尺寸界线落在真实轮廓上
- 轮廓分段尺寸：水平/垂直边、斜边主投影（GB/T 4458.4）
- 孔/圆直径 + 定位尺寸链；重复孔径合并为 `N×Ød` 注记，代表孔保留 Ø 引线
- 圆角/圆弧半径 R（同一圆只标一次，圆心/半径保真）
- 布局避让：按文字框判据外推，标注不压字、不飞出图纸

### 关键修复

- 圆弧标注三层几何 bug：面提取 bulge 错位、面追踪按弦方向转向、清洗取整致圆心漂移
- 总体尺寸界线不再悬空（圆弧端头用真实轮廓边界点）
- 共享边/重复斜边不重复标注；连体小面不重复标总体
- 图层颜色：标注红色、中心线黄色、清洗中间层默认关闭

### 实测

- test2（SW 导出对称圆头板件）：34 尺寸 + 2 注记，文字撞车 0
- test.dwg（扫描碎片件，ADIMCLN 调紧后）：130 尺寸 + 64 注记，文字撞车 0

### 路线

- 多轮廓相交 / 复杂装配图鲁棒性
- 对称与阵列特征的 N× 合并、国标化排版打磨
- 3D(STEP)、MLLM 语义理解（远期）
